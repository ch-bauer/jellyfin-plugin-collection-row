name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0.x

      - name: Build
        run: dotnet publish src/Jellyfin.Plugin.CollectionRow -c Release -o publish

      - name: Package
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mkdir package
          cp publish/Jellyfin.Plugin.CollectionRow.dll package/
          cp meta.json package/
          (cd package && zip "../collection-row_${VERSION}.zip" ./*)
          echo "MD5 checksum for manifest.json:"
          md5sum "collection-row_${VERSION}.zip"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          files: collection-row_*.zip
          generate_release_notes: true
